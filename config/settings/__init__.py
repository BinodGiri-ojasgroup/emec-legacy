# Settings are selected explicitly via DJANGO_SETTINGS_MODULE, e.g.:
#   config.settings.dev   (local development — default in manage.py)
#   config.settings.prod  (production, set in the deployment environment)
#
# This file intentionally stays empty so `import config.settings` never
# silently activates the wrong environment.
